/**
 * Product and marketing media deliberately begin empty.
 * Uploading and assigning media through the existing Media Management flow is
 * the only way records enter the client-side repository on a fresh install.
 */
export const SEED_MEDIA = [];
export default SEED_MEDIA;
